<Query Kind="Expression" />

// the XML files for this course


// BigStar.xsd (the schema file)
// BigStar.xml (data, uses the schema file)
// BigStar-WithPrefix.xml (data, uses schema via namespace prefix)

// BigStar-NoSchema.xml (same data, no schema)
// BigStar-MissingXML.xml (file with missing elements and data).
// CardSimple.xml (simplified data, one Card)
// Northwind.xml (a few customer and order values)
// WebColors.xml (sample of standard webcolors, used in the code challenges)